# API Keys for AI Browser Automation

# Required: Hyperbrowser API key for cloud browser automation with Claude Computer Use
HYPERBROWSER_API_KEY=hb_348d57c622a361e96132f1c8737d

# Optional: Anthropic API key for custom Claude usage (if you want to use your own Claude quota)
# If not provided, Hyperbrowser will use their built-in Claude access
ANTHROPIC_API_KEY=your_anthropic_key_here

# Instructions:
# 1. Copy this file to .env
# 2. Get your Hyperbrowser API key from: https://hyperbrowser.ai
# 3. Optionally get Anthropic API key from: https://console.anthropic.com (only if you want custom quota)
# 4. Replace the placeholder values with your actual API keys

# Note: OpenRouter API is not needed for Claude Computer Use - Hyperbrowser has built-in Claude access

# Note: The system is configured for no-login mode using only public job sites.
# Login-required platforms (LinkedIn, Indeed) have been removed from the configuration.
# All supported platforms are accessible without authentication.

# JOB AUTOMATION SETTINGS
# Control job search and application behavior

# Job Search Limits
MAX_JOBS_TO_FIND=50
MAX_JOBS_PER_PLATFORM=10
MAX_CONCURRENT_BROWSERS=5
MAX_APPLICATIONS_PER_RUN=10
MAX_APPLICATIONS_PER_PLATFORM=3

# Browser Automation Settings
SEARCH_MAX_STEPS=30
APPLICATION_MAX_STEPS=40
TEST_MAX_STEPS=10

# Timing Controls (in seconds)
APPLICATION_DELAY_SECONDS=30
RATE_LIMIT_DELAY_MIN=1
RATE_LIMIT_DELAY_MAX=3
SESSION_TIMEOUT_SECONDS=1200

# Quality Controls
DUPLICATE_DETECTION_ENABLED=true
ENABLE_SCREENSHOTS=true
ENABLE_SESSION_RECORDINGS=true

# Job Generation Controls (for testing/simulation)
JOBS_PER_SEARCH_MIN=1
JOBS_PER_SEARCH_MAX=3